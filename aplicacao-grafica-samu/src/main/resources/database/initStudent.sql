CREATE TABLE IF NOT EXISTS Student
(
    id                INTEGER,
    requestedLectures VARCHAR,
    enrollLectures    VARCHAR,
    course            VARCHAR,
    semester          VARCHAR
);
